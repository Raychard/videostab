# 环境与缓存
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# 数据与产物 (体积大, 可由脚本重建)
data/
*.mp4
*.avi
*.npz

# 权重: 出厂 .pt 仅 32~64KB 且是复现评测结果的必需品, 纳入版本控制;
# 训练中间检查点体积大, 仍排除
weights/*.ckpt
weights/*.pth
weights/*.tar

# 本地配置
.claude/settings.local.json

# 系统杂项
.DS_Store
Thumbs.db
